# Chef's Kiss Backend - Environment Variables
#
# For local development: Copy this file to .env and fill in your values
# For Railway deployment: Set these in Railway Dashboard → Service → Variables
#
# See RAILWAY_SETUP.md for detailed Railway deployment instructions

# =============================================================================
# Supabase Configuration
# =============================================================================
# Get these from: Supabase Dashboard → Project Settings → API
# IMPORTANT: Use service_role key, NOT anon key!

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here
SUPABASE_ANON_KEY=your-anon-key-here

# =============================================================================
# Redis Configuration
# =============================================================================
# For local development (localhost):
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_DB=0
#
# For Railway deployment:
# Railway automatically provides REDIS_URL when Redis service is added
# No manual configuration needed - just link Redis service to backend service
REDIS_URL=redis://localhost:6379/0

# =============================================================================
# JWT Configuration
# =============================================================================
# Generate a secure JWT secret with: openssl rand -hex 32
# Keep this secret! Do not commit to git!

JWT_SECRET_KEY=your-secret-key-here-generate-new-one
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=43200

# =============================================================================
# CORS Origins (comma-separated)
# =============================================================================
# Local development:
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,https://jdudgeon1993.github.io
#
# Production (Railway/Netlify/etc):
# Add your frontend domain here:
# CORS_ORIGINS=http://localhost:3000,http://localhost:8080,https://jdudgeon1993.github.io

# =============================================================================
# Environment
# =============================================================================
# Options: development, production
# Railway: Set to "production"
ENVIRONMENT=development

# =============================================================================
# Port (Optional)
# =============================================================================
# Railway automatically sets PORT, no need to configure
# For local: defaults to 8000
# PORT=8000
